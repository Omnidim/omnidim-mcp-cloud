name: ci

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: omni
          POSTGRES_PASSWORD: omni
          POSTGRES_DB: omni_mcp
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U omni"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 5

    env:
      DATABASE_URL: postgresql+asyncpg://omni:omni@localhost:5432/omni_mcp
      PUBLIC_BASE_URL: http://localhost:8000
      DASHBOARD_BASE_URL: http://localhost:3000
      ODOO_INTERNAL_BASE_URL: http://localhost:8069
      ODOO_INTERNAL_SHARED_SECRET: ci-shared-secret
      TOKEN_SIGNING_KEY: ci-signing-key-padded-to-32-bytes!!

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version-file: .python-version
          cache: pip
      - run: pip install --upgrade pip && pip install -e ".[dev]"
      - run: ruff check .
      - run: mypy app
      - run: alembic upgrade head
      - run: pytest --cov=app --cov-report=term-missing

  docker:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v6
        with:
          context: .
          load: true
          tags: omnidim-mcp-cloud:ci
      - name: Smoke test the image
        run: |
          docker run -d --name smoke \
            -e DATABASE_URL=postgresql+asyncpg://invalid/db \
            -e PUBLIC_BASE_URL=http://localhost:8000 \
            -e DASHBOARD_BASE_URL=http://localhost:3000 \
            -e ODOO_INTERNAL_BASE_URL=http://localhost:8069 \
            -e ODOO_INTERNAL_SHARED_SECRET=smoke \
            -e TOKEN_SIGNING_KEY=smoke-signing-key-padded-to-32-bytes \
            -p 8000:8000 \
            omnidim-mcp-cloud:ci
          for i in $(seq 1 30); do
            if curl -fsS http://localhost:8000/healthz; then exit 0; fi
            sleep 1
          done
          docker logs smoke
          exit 1

  # Deploy job intentionally absent: production EC2 not yet provisioned.
  # When ready, add a `deploy` job that:
  #   - runs on `if: startsWith(github.ref, 'refs/tags/v')`
  #   - uses `environment: production`
  #   - SSHes to mcp.omnidim.io, pulls the tagged image, restarts the service
  # Keep test + docker as required gates ahead of it.
